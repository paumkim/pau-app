import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Basic smoke test — will expand as app grows
    expect(true, isTrue);
  });
}
